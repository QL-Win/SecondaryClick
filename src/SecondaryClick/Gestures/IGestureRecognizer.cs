namespace SecondaryClick.Gestures;

/// <summary>
/// Interface for gesture recognizers in the SecondaryClick application.
/// Gesture recognizers detect user gestures (like mouse + keyboard modifiers or touchpad gestures)
/// and trigger appropriate click events.
/// </summary>
public interface IGestureRecognizer : IDisposable
{
}
