<Project Sdk="Microsoft.NET.Sdk">
    <PropertyGroup>
        <TargetFrameworks>netstandard2.0;netstandard2.1;net462;net472;net48;net481;net5.0;net6.0;net7.0;net8.0;net9.0;net10.0;</TargetFrameworks>
        <LangVersion>preview</LangVersion>
        <UseWindowsForms>false</UseWindowsForms>
        <Version>1.0.3.0</Version>
        <AssemblyTitle>SecondaryClick.MouseKeyHook</AssemblyTitle>
        <AssemblyName>System.MouseKeyHook</AssemblyName>
        <RootNamespace>System.MouseKeyHook</RootNamespace>
        <PackageId>$(AssemblyTitle)</PackageId>
        <Platforms>x86;x64;ARM64</Platforms>
        <Authors>QL-Win</Authors>
        <Company>QL-Win</Company>
        <RepositoryUrl>https://github.com/QL-Win/SecondaryClick</RepositoryUrl>
        <PackageProjectUrl>https://github.com/QL-Win/SecondaryClick</PackageProjectUrl>
        <RepositoryType>git</RepositoryType>
        <PackageTags>keyboard mouse hook</PackageTags>
        <PackageLicenseExpression>MIT</PackageLicenseExpression>
        <Description>This library allows you to tap the keyboard and mouse, to detect and record their activity even when an application is inactive and runs in the background.</Description>
        <PackageIcon>Logo.png</PackageIcon>
        <PackageReadmeFile>README.md</PackageReadmeFile>
        <DebugType>portable</DebugType>
        <DebugSymbols>true</DebugSymbols>
        <IncludeSymbols>true</IncludeSymbols>
        <EnableWindowsTargeting>true</EnableWindowsTargeting>
        <GeneratePackageOnBuild>true</GeneratePackageOnBuild>
        <SymbolPackageFormat>snupkg</SymbolPackageFormat>
    </PropertyGroup>

    <ItemGroup>
        <None Include="..\..\branding\Logo.png">
            <Pack>True</Pack>
            <PackagePath>\</PackagePath>
        </None>
        <None Include="..\..\README.md">
            <Pack>True</Pack>
            <PackagePath>\</PackagePath>
        </None>
    </ItemGroup>

</Project>